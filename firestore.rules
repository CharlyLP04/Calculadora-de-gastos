rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /clara_users/{userId} {
      allow read, write: if true;
      match /{allChildren=**} {
        allow read, write: if true;
      }
    }
  }
}
